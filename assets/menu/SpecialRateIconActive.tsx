import { Image } from 'react-native';

export const SpecialRateIconActive = ({ width, height }: any) => {
  return (
    <Image source={require("../icon/ic_special_fill.png")} style={{ width, height }} />
  );
};
