import { Image } from "react-native";

export const AkunIconActive = ({ width, height }: any) => {
  return (
    <Image source={require("../icon/ic_profil_fill.png")} style={{ width, height }} />
  );
};
