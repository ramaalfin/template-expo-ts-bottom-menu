import { Image } from "react-native";

export const PipelineIconInactive = ({ width, height }: any) => {
  return (
    <Image source={require("../icon/ic_prospek.png")} style={{ width, height }} />
  );
};
