import { Image } from "react-native";

export const PipelineIconActive = ({ width, height }: any) => {
  return (
    <Image source={require("../icon/ic_prospek_fill.png")} style={{ width, height }} />
  );
};
