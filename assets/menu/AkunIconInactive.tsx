import { Image } from "react-native";

export const AkunIconInactive = ({ width, height }: any) => {
  return (
    <Image source={require("../icon/ic_profil.png")} style={{ width, height }} />
  );
};
