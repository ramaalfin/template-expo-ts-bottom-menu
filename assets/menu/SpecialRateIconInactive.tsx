import { Image } from 'react-native';

export const SpecialRateIconInactive = ({ width, height }: any) => {
  return (
    <Image source={require("../icon/ic_special.png")} style={{ width, height }} />
  );
};
